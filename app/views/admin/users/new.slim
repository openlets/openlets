.row
  .large-12.columns
    h1.bold.text-center= t('forms.new_resource', name: t("activerecord.models.#{resource_class.name.downcase}", count: 1))
    = render "form", attributes: [:email, :name, :about, :locale, :password, :password_confirmation]